# Core dependencies
python-dotenv>=1.0.0
pyyaml>=6.0

# MCP Protocol
mcp>=1.0.0

# LLM Providers
openai>=1.0.0

# Embedding
# openai (already included above)

# Vector Store
chromadb>=0.4.0

# PDF Loading
pymupdf>=1.24.0       # PDF text + positional image extraction (used by PdfLoader)
markitdown>=0.1.0     # Optional, high-level conversion utility

# Splitter
langchain-text-splitters>=0.2.0

# Reranker (optional, when using cross_encoder backend)
# sentence-transformers>=2.2.0

# Dashboard
streamlit>=1.30.0
pandas>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
